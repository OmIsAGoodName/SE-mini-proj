To downloadrun in cmd: git clone https://github.com/<your-username>/SE-mini-proj.git
and in the directory where you downloaded, in cmd, cd to that folder and run: pip install -r requirements.txt
after that, run the app.py: python app.py
itll give an adress. if you directly click generate tt it'll generate the timetable from existing dataset. or, you can add more faculty etc.
